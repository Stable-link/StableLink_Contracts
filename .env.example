# Deploy to Etherlink Shadownet (get test XTZ at https://shadownet.faucet.etherlink.com/).
# Etherlink needs ~18.5M gas for deploy; --gas-estimate-multiplier 2000 makes broadcast use ~20M gas:
#   forge script script/Deploy.s.sol --rpc-url shadownet --broadcast --private-key $PRIVATE_KEY --gas-estimate-multiplier 2000
# Deploy to Etherlink Mainnet:
#   forge script script/Deploy.s.sol --rpc-url mainnet --broadcast --private-key $PRIVATE_KEY --gas-estimate-multiplier 2000
#
# Wallet (required for --broadcast)
PRIVATE_KEY=0x...

# Optional: platform fee recipient (defaults to deployer)
# PLATFORM_FEE_RECIPIENT=0x...

ETHERLINK_RPC_URL=https://node.shadownet.etherlink.com
CHAIN_ID=127823

# After deploying TestUSDC, set its address here for Faucet deploy + funding
TEST_USDC_ADDRESS=
# After deploying Faucet, set its address here for funding script
FAUCET_ADDRESS=
# Optional: amount to send to faucet (default 100_000 * 10^6 = 100k tUSDC)
# FAUCET_FUND_AMOUNT=100000000000
